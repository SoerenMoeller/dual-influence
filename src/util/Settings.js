const SETTINGS = {
    scene: null,
    camera: null,
    controls: null,

    showGrid: false,
    showConnector: false,
    gridSize: 10,
    example: "example2",
    interactiveMode: false,
    opacity: 0.3,
    threshold: 50
}

export default SETTINGS;